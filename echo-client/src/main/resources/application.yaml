server:
  port: 9001

spring:
  application:
    name: demo-echo-client

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    lease-renewal-interval-in-seconds: 5
